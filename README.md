# MeawMeeCakeProject
1.ติดตั้ง Package BackEnd
    1.1 npm init -y
    1.2 npm install express dotenv cors mongoose
    1.3 npm install nodemon --save-dev
    1.4 ติดตั้ง Google Login (Passport.js)
        1.4.1 npm install passport passport-google-oauth20


2. ติดตั้ง dependency FrontEnd
    2.1 npm install
    2.2 npm install axios react-router-dom react-bootstrap bootstrap
